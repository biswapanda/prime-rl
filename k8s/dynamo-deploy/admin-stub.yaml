# kubectl apply -f admin-stub.yaml -n biswa-fe
# Stub for /pause, /resume, /update_weights endpoints Dynamo doesn't implement yet
apiVersion: v1
kind: ConfigMap
metadata:
  name: admin-stub-script
  namespace: biswa-fe
data:
  admin_stub.py: |
    from http.server import HTTPServer, BaseHTTPRequestHandler
    class H(BaseHTTPRequestHandler):
        def do_POST(self):
            n = int(self.headers.get("Content-Length", 0))
            body = self.rfile.read(n) if n else b""
            print(f"[stub] POST {self.path} body={body[:200]}")
            self.send_response(200)
            self.end_headers()
            self.wfile.write(b"OK")
        def do_GET(self):
            self.send_response(200)
            self.end_headers()
            self.wfile.write(b"OK")
    HTTPServer(("0.0.0.0", 8001), H).serve_forever()
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-stub
  namespace: biswa-fe
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-stub
  template:
    metadata:
      labels:
        app: admin-stub
    spec:
      containers:
        - name: stub
          image: python:3.12-slim
          command: ["python3", "/scripts/admin_stub.py"]
          ports:
            - containerPort: 8001
          volumeMounts:
            - name: script
              mountPath: /scripts
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
      volumes:
        - name: script
          configMap:
            name: admin-stub-script
---
apiVersion: v1
kind: Service
metadata:
  name: admin-stub
  namespace: biswa-fe
spec:
  selector:
    app: admin-stub
  ports:
    - port: 8001
      targetPort: 8001
